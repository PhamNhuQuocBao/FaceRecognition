import styled from "styled-components";
import { Form } from "antd";

export const FormStyled = styled(Form)`
  & .layout-btn .ant-form-item-control-input-content {
    display: flex;
    justify-content: end;
  }
`;
